<Window x:Class="Vetcare.Presentacion.Facturas.WindowSelectorConcepto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vetcare - Seleccionar Concepto" Height="650" Width="850"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" 
        Background="#F4F6F7" AllowsTransparency="False" WindowStyle="ThreeDBorderWindow"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ColumnTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="12,0"/>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8F9F9"/>
            <Setter Property="Foreground" Value="#566573"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#E67E22"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#EBF5FB"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#2C3E50"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#EBF5FB"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#2C3E50"/>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9F9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="Transparent" BorderThickness="0" SnapsToDevicePixels="True">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="SELECCIONAR CONCEPTO / SERVICIO" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <Rectangle Height="4" Fill="#E67E22" HorizontalAlignment="Left" Width="60" Margin="2,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#D5DBDB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="BUSCAR POR NOMBRE" FontSize="11" FontWeight="Bold" Foreground="#85929E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtBusqueda" TextChanged="Filtro_Changed" Height="38" VerticalContentAlignment="Center" Padding="10,0" FontSize="15"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="TIPO DE CONCEPTO" FontSize="11" FontWeight="Bold" Foreground="#85929E" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbTipoFiltro" Height="38" SelectionChanged="Filtro_Changed" VerticalContentAlignment="Center" FontSize="15">
                        <ComboBoxItem Content="Todos" IsSelected="True"/>
                        <ComboBoxItem Content="Producto"/>
                        <ComboBoxItem Content="Servicio"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Orientation="Horizontal">
                    <Button x:Name="btnNuevoProducto" Style="{StaticResource RoundedButtonStyle}" Click="btnNuevoProducto_Click" Content="+ Producto" Background="#27AE60" Foreground="White" Width="100" Height="38" Margin="0,0,8,0" Cursor="Hand" FontWeight="Bold"/>
                    <Button x:Name="btnNuevoServicio" Style="{StaticResource RoundedButtonStyle}" Click="btnNuevoServicio_Click" Content="+ Servicio" Background="#27AE60" Foreground="White" Width="100" Height="38" Cursor="Hand" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#D5DBDB" BorderThickness="1" Padding="5">
            <DataGrid x:Name="dgConceptos" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionMode="Single" MouseDoubleClick="dgConceptos_MouseDoubleClick"
                      HeadersVisibility="Column" GridLinesVisibility="None" 
                      Background="White" BorderThickness="0"
                      RowHeight="50" ColumnHeaderStyle="{StaticResource HeaderStyle}"
                      SelectionUnit="FullRow" Focusable="False">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="TIPO" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border x:Name="brdTipo" CornerRadius="6" Padding="10,4" 
                                        HorizontalAlignment="Center" VerticalAlignment="Center" 
                                        Background="#BDC3C7">
                                    <TextBlock Text="{Binding Tipo}" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Tipo}" Value="Producto">
                                        <Setter TargetName="brdTipo" Property="Background" Value="#3498DB"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Tipo}" Value="Servicio">
                                        <Setter TargetName="brdTipo" Property="Background" Value="#9B59B6"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="NOMBRE DEL CONCEPTO" Binding="{Binding Nombre}" Width="*" 
                                        ElementStyle="{StaticResource ColumnTextStyle}"/>

                    <DataGridTextColumn Header="IVA" Binding="{Binding IvaPorcentaje, StringFormat={}{0}%}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource ColumnTextStyle}">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="PRECIO" Binding="{Binding Precio, StringFormat={}{0:N2}€}" Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource ColumnTextStyle}">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,20,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="CANCELAR" Style="{StaticResource RoundedButtonStyle}" Click="btnCancelar_Click" Height="45" Background="#BDC3C7" Foreground="Black" FontWeight="SemiBold" Cursor="Hand"/>

            <Button Grid.Column="2" x:Name="btnSeleccionar" Style="{StaticResource RoundedButtonStyle}" Content="SELECCIONAR Y AÑADIR A FACTURA" Click="btnSeleccionar_Click" Height="45" Background="#2E86C1" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>