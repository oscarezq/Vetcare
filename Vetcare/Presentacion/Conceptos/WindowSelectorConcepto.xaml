<Window x:Class="Vetcare.Presentacion.Facturas.WindowSelectorConcepto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Concepto" Height="450" Width="600"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Seleccionar Producto o Servicio" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtBusqueda" TextChanged="txtBusqueda_TextChanged" Height="25"/>
        </DockPanel>

        <DataGrid x:Name="dgConceptos" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="dgConceptos_MouseDoubleClick"
                  Margin="0,10" VerticalGridLinesBrush="LightGray" HorizontalGridLinesBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Precio Base" Binding="{Binding PrecioBase, StringFormat={}{0:N2} €}" Width="100"/>
                <DataGridTextColumn Header="IVA" Binding="{Binding IvaPorcentaje, StringFormat={}{0}%}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancelar" Content="Cancelar" Width="100" Margin="5" Click="btnCancelar_Click"/>
            <Button x:Name="btnSeleccionar" Content="Seleccionar" Width="100" Margin="5" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" Click="btnSeleccionar_Click"/>
        </StackPanel>
    </Grid>
</Window>