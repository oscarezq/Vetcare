<UserControl x:Class="Vetcare.Presentacion.Mascotas.UC_HistorialClinico"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="RowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1ABC9C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#16A085"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CenterVerticalStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,30">
            <TextBlock Text="Historial Clínico" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
            <Border Height="4" Width="50" Background="#1ABC9C" HorizontalAlignment="Left" Margin="0,5,0,10"/>
            <TextBlock Text="Consulta el registro histórico de atenciones del paciente." Foreground="#7F8C8D" FontSize="14"/>
        </StackPanel>

        <DataGrid x:Name="dgHistorial" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Background="White" 
                  BorderThickness="1" 
                  BorderBrush="#EAECEE" 
                  RowHeight="50" 
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#F2F4F4"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  HeadersVisibility="Column" 
                  RowHeaderWidth="0" 
                  HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#FBFCFC"/>
                    <Setter Property="Foreground" Value="#34495E"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#1ABC9C"/>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#F4F6F7"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" 
                                    Binding="{Binding FechaHora, StringFormat=dd/MM/yyyy HH:mm}" 
                                    ElementStyle="{StaticResource CenterVerticalStyle}"
                                    Width="1.5*"/>

                <DataGridTextColumn Header="Motivo de Consulta" 
                                    Binding="{Binding Motivo}" 
                                    ElementStyle="{StaticResource CenterVerticalStyle}"
                                    Width="3*"/>

                <DataGridTextColumn Header="Veterinario" 
                                    Binding="{Binding NombreVeterinario}" 
                                    ElementStyle="{StaticResource CenterVerticalStyle}"
                                    Width="2*"/>

                <DataGridTemplateColumn Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver Registro" 
                                    Style="{StaticResource RowButtonStyle}"
                                    Click="btnVerDetalle_Click"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>